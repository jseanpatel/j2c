

function storeGrade(user) {
  var elem = document.getElementById()
  print(user)
}

function storeQuestion(needs) {
  var elem = document.getElementById()
  calendar[].add(needs)
}

function storeState(state) {
  var elem = document.getElementById()
calendar[].add(stateInfo)
}
